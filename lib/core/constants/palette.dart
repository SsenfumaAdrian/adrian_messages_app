import 'package:flutter/material.dart';

/// Centralized color palette for the auth experience.
class Palette {
  static const Color background = Color(0xFF041024);
  static const Color glass = Color(0x40FFFFFF); // translucent white
  static const Color accentCyan = Color(0xFF2DE2E6);
  static const Color accentBlue = Color(0xFF0F6FFF);
  static const Color accentIndigo = Color(0xFF163A72);
  static const Color textPrimary = Color(0xFFE9ECF5);
  static const Color textMuted = Color(0xFF9BB0D1);
}
