import 'package:flutter/material.dart';

/// Centralized color palette for the auth experience.
class Palette {
  static const Color background = Color(0xFF060D1F);
  static const Color glass = Color(0x33FFFFFF); // translucent white
  static const Color accentCyan = Color(0xFF2DE2E6);
  static const Color accentBlue = Color(0xFF0F6FFF);
  static const Color accentPurple = Color(0xFF8A5DFF);
  static const Color textPrimary = Color(0xFFE9ECF5);
  static const Color textMuted = Color(0xFF8FA0C2);
}
